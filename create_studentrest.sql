CREATE TABLE _student (
                      id     BIGSERIAL PRIMARY KEY,
                      name   TEXT,
                      age    INTEGER,
                      email  TEXT
	              );
